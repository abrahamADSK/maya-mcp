# DEPRECATED — No longer needed.
#
# The MCP server now injects a guarded block into Maya's userSetup.py
# on first connect, which adds the project root to sys.path automatically.
# The menu and panel are also installed via the MCP server's auto-setup.
#
# This file is kept only for backward compatibility references.
# It can be safely deleted.
