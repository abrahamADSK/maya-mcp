# ── Environment & secrets ────────────────────────────────────
.env
*.env.local

# ── Per-user runtime config (hostnames, backend choice, etc.) ─
# Only config.example.json is committed. The live config.json is
# populated by each user/machine and must never enter the repo.
src/maya_mcp/config.json

# ── Pipeline outputs ─────────────────────────────────────────
reference/3d_output/
renders/

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv*/
*.egg-info/
dist/
build/

# ── macOS ────────────────────────────────────────────────────
.DS_Store
.AppleDouble

# ── Cowork skill bundles ─────────────────────────────────────
*.skill

# ── Temp scripts ─────────────────────────────────────────────
*.command

# ── Claude / RAG (generated) ─────────────────────────────────
.claude/
.mcp.json

# ── Editor ───────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Maya ─────────────────────────────────────────────────────
*.ma
*.mb
*.mayaSwatches/

# ── Large model files (weights should never be committed) ────
*.safetensors
*.ckpt
*.bin
hf_models/
logs/
src/maya_mcp/rag/index/
src/maya_mcp/rag/corpus.json
